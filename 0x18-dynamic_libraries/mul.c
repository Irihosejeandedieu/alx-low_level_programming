#include <stdlib.h>
#include <stdio.h>

/**
 * mul - multiply two integers
 * @a :the first value
 * @b:the second integer
 * Return:multplication results
 */

int mul(int a, int b)
{
	return (a * b);
}
